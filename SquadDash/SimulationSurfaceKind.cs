namespace SquadDash;

/// <summary>
/// Identifies which panel surface a simulation artifact targets.
/// </summary>
internal enum SimulationSurfaceKind
{
    Plan,
    Notes,
    Tasks,
    Approvals,
    Inbox,
    Loop
}
