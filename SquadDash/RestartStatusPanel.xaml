<UserControl x:Class="SquadDash.RestartStatusPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource AlertSurface}"
             Padding="0"
             Height="Auto"
             VerticalAlignment="Top">
    <UserControl.Resources>
        <Style x:Key="RestartStatusCloseButtonStyle" TargetType="Button">
            <Setter Property="OverridesDefaultStyle" Value="True" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="Transparent"
                                BorderThickness="0"
                                Padding="0">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="{DynamicResource HoverSurface}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="{DynamicResource PanelSurface}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Height="32" Margin="0,-2,0,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <!-- Message Text -->
        <TextBlock Grid.Column="0"
                   Text="Restart requested, waiting for current turn to complete before shutting down"
                   Foreground="{DynamicResource AlertBodyText}"
                   FontSize="12"
                   VerticalAlignment="Center"
                   HorizontalAlignment="Left"
                   Margin="12,0,12,0" />

        <!-- Close Button (X) -->
        <Button Grid.Column="1"
                Content="✕"
                Width="32"
                Height="32"
                Background="{DynamicResource AlertSurface}"
                Foreground="{DynamicResource AlertBodyText}"
                BorderThickness="0"
                Padding="0"
                FontSize="14"
                FontWeight="Bold"
                Cursor="Hand"
                Click="CloseButton_Click"
                VerticalAlignment="Center"
                HorizontalAlignment="Right"
                Style="{StaticResource RestartStatusCloseButtonStyle}" />
    </Grid>
</UserControl>
